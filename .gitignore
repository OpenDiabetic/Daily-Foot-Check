# Dependencies / build
node_modules/
build/
.cache/
# NOTE: dist/ios/ is TRACKED on purpose — it holds the generated iOS QC constants
# checked by invariant #5 (regenerate + git diff --exit-code). Do not ignore dist/.

# Local / secrets
.env
*.local

# Editors / OS
.DS_Store
*.swp
.idea/
.vscode/

# Docker / Caddy runtime data
caddy_data/
